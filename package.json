{
  "name": "cestovatelsky-denik",
  "version": "1.0.0",
  "private": true,
  "description": "Interní aplikace — postaveno na struktuře Fapalix",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
